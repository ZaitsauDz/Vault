import SwiftUI

extension Color {
    static let vaultPrimary = Color.blue
    static let vaultBackground = Color(.systemBackground)
    static let vaultSecondary = Color(.secondarySystemBackground)
    static let vaultText = Color.primary
    static let vaultSecondaryText = Color.secondary
    
    // Category colors
    static let watchCategory = Color.red
    static let readCategory = Color.blue
    static let buyCategory = Color.green
    static let cookCategory = Color.orange
}
